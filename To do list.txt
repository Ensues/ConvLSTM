Hannah To do list:

Medium Effort, High Reward:
- Validation metrics per class: Track precision/recall per class during training 

Big Projects (High Impact but Time-Intensive):
- TorchScript compilation: torch.jit.script() for faster inference - Like ONNX but it appears to apparently improve inference speed so could be nice to have
- ONNX export: Run on optimized runtime - Unsure if needed, ngl parang unnecessary wambo jumbo but could help maybe
# Choose either TorchScript compilation, ONNX export, or to not do this at all
# I'm also unsure if this can apply to mobile or not

Nice-to-Have:
- Resume training: Save/load optimizer state and epoch number 
- Checkpointing: Save model every N epochs, not just best 

Will come back to maybe:
- Mixed precision training: Use torch.cuda.amp for faster training - needs GPU, we have none right now, wag na muna

Wag muna, pag may premium nlng:
- Increase workers - failed due windows, free tier colab can't handle current model with this
- Persistent workers - failed due windows, free tier colab can't handle current model with this
- Pin memory - A specialized, non-pageable memory area in RAM that prevents the Operating System (OS) from swapping that data to disk

Eric Finished List:
- Gradient clipping: Prevent exploding gradients
- Early stopping: Stop training when validation stops improving
- Dropout layers: Add dropout in the linear layer to prevent overfitting
- Track inference time during validation
- Learning rate scheduling: Reduce LR when validation plateaus (ReduceLROnPlateau)
- Delete intermediate tensors: Use del and clear unnecessary variables
- Gradient accumulation: Simulate larger batches with less memory
- Video preprocessing: Pre-extract and cache frames to disk
- Dynamic Cache Manager with LRU Eviction - Adapting caching logic for large scale training
- Documentation chene
- .yaml - human-readable data serialization language for config documentation but could also be used in the code


Eric Implemented but Unsure List
- Tensorboard logging: Track loss curves, accuracy, learning rate - Unsure if needed, located at proto 3
- Batch normalization: Add after ConvLSTM layers for training stability - I did but it gave worse results, further discussed in the proto 2 readme 
# Pa reimplement if tingin mo need Hannah
# Sa batch normalization it made accuracy worse, may discussion sa proto 2 readme rin to help you decide
